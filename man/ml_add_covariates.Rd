% Generated by hand for documentation-only update
\name{ml_add_covariates}
\alias{ml_add_covariates}
\title{Join climate covariates onto harmonized data}
\description{
Adds climate covariates to the harmonized observation table using a join mode
such as plot-date or site-year. This step does not modify site standardizers.
}
\usage{
ml_add_covariates(
  h,
  covariates,
  locations,
  join = c("plot_date", "site_date", "plot_year", "site_year"),
  keep = "all",
  warn_join_loss = TRUE
)
}
\arguments{
  \item{h}{An \code{ml_harmonized} object.}
  \item{covariates}{A covariate spec or list of specs.}
  \item{locations}{An sf object with \code{site_id}, \code{plot_id}, and geometry.}
  \item{join}{Join mode for covariates.}
  \item{keep}{Whether to keep all rows or only matched rows.}
  \item{warn_join_loss}{Warn if the join introduces missing values.}
}
\value{
An updated \code{ml_harmonized} object with covariates and join logs.
}
