## Standardize Data

The scripts in this folder perform necessary site-specific wrangling to get all data files into a comparable format. Each file assumes you have downloaded the necessary inputs from the Environmental Data Initiative; run `00_download.r` to ensure this is the case.
